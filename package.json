{
  "name": "catchy-fabric-market",
  "version": "1.0.0",
  "description": "A bilingual marketplace for fabrics and accessories",
  "main": "index.js",
  "scripts": {
    "build": "flutter build web",
    "sync": "npx cap sync",
    "android": "npx cap run android",
    "ios": "npx cap run ios"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/android": "^5.0.0",
    "@capacitor/ios": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
